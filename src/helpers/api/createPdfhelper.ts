// Imports


export const createPDFHelper = (idProduct: Number, quantity : Number) => {
    let getProduct = `SELECT * FROM products WHERE idProduct = ${idProduct}`

    //procesar venta
    // Obtener idProduct, Nombre, purchasePrice, 
    
    //crear pdf

}